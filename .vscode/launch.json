{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "node-terminal",
            "request": "launch",
            "command": ".vscode/app.bat",
            "cwd": "${workspaceFolder}"
        }
    ]
}
